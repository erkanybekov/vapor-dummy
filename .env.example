# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=vapor_username
DATABASE_PASSWORD=vapor_password
DATABASE_NAME=vapor_database

# Environment
VAPOR_ENV=development

# JWT Configuration (use secure random keys in production)
# Generate with: ssh-keygen -t rsa -b 4096 -m PEM -f jwtRS256.key
# Public key: openssl rsa -in jwtRS256.key -pubout -outform PEM -out jwtRS256.key.pub

# Server Configuration
PORT=8080
HOST=127.0.0.1
